services:
  caddy:
    build: .
    image: ghcr.io/x13a/caddy-proxy-host:latest
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
      - 443:443/udp
    volumes:
      - ./config/:/etc/caddy/:ro
      - ./www/:/www/:ro
      - caddy_data:/data
      - caddy_config:/config
    container_name: caddy
    networks:
      - proxy
    labels:
      com.centurylinklabs.watchtower.enable: "true"
    env_file:
      - ./caddy.env

volumes:
  caddy_data:
    name: caddy_data
  caddy_config:
    name: caddy_config
